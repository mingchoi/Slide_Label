// Generated by CoffeeScript 1.6.2
(function() {
  var SLabel;

  $.fn.extend({
    Slide_Label: function(element) {
      return new SLabel(this, element);
    }
  });

  SLabel = (function() {
    function SLabel(link, element) {
      var gapBox,
        _this = this;

      gapBox = $("<div class='Slide_Label_Gap' style='display: none; position: absolute; padding-left: 10px; padding-right: 10px'></div>");
      gapBox.css('width', parseInt(element.css('width').substr(0, element.css('width').length - 2), 10) + 0 + 'px');
      gapBox.append(element);
      $('body').append(gapBox);
      element.hide();
      link.mouseover(function() {
        _this.reposition(link, gapBox);
        return gapBox.fadeIn(100, function() {
          return element.slideDown(400);
        });
      });
      gapBox.mouseleave(function() {
        return element.slideUp(200, function() {
          return gapBox.fadeOut(100);
        });
      });
    }

    SLabel.prototype.reposition = function(link, gapBox) {
      gapBox.css('left', link.offset().left + link.width() * 0.5 - gapBox.width() * 0.5 + 'px');
      return gapBox.css('top', link.offset().top + link.height() + 'px');
    };

    return SLabel;

  })();

}).call(this);
